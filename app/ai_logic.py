# AI logic for seat suggestions
def suggest_seats(train):
    # Example AI logic for seat suggestions
    if train.seats_available > 0:
        return "Seats available"
    else:
        return "No seats available"
